<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Toolbar主题设置-->
    <style name="ToolBarTheme" parent="AppTheme">
        <item name="android:textColorSecondary">#ffffff</item><!--左侧 右侧图标颜色-->
        <item name="titleTextColor">@android:color/white</item><!--标题颜色-->
        <item name="android:colorBackground">#ffffff</item><!--Menu背景颜色-->
        <item name="android:textColor">#000000</item><!--menu文字颜色-->

        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item> <!--新增一个item，用于控制menu-->
        <!-- <item name="android:actionOverflowButtonStyle">@style/OverflowButtonStyle</item>-->
    </style>
    <!--修改toolbar弹出菜单的位置-->
    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>  <!--把该属性改为false即可使menu位置位于toolbar之下-->
    </style>
    <!--修改toolbar菜单按钮的style-->
    <style name="OverflowButtonStyle" parent="@android:style/Widget.ActionButton.Overflow">
        <item name="android:src">@android:drawable/ic_menu_upload_you_tube</item>
    </style>-->
    <!--toolbar菜单按钮的颜色-->
    <style name="ActionButton.Overflow" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
    </style>
</resources>
